# RDAP Configuration
RDAP_CONCURRENCY=5
RDAP_TIMEOUT_SECONDS=10

# Database
DB_PATH=/data/domains.db

# Frontend port (host)
FRONTEND_PORT=80

# Default TLDs (comma-separated)
DEFAULT_TLDS=com,io,ai,co,net,org
